{
  "devDependencies": {
    "eslint": "^7.26.0",
    "husky": "^6.0.0",
    "lint-staged": "^11.0.0",
    "prettier": "^2.3.0"
  },
  "lint-staged": {
    "./api/src/**/*.js": [
      "prettier --config ./.prettierrc",
      "eslint --fix"
    ],
    "./front/src/**/*.{js,jsx}": [
      "prettier --config ./.prettierrc",
      "eslint --fix"
    ]
  },
  "scripts": {
    "prepare": "husky install"
  }
}
