import { Meta, Story, Canvas } from '@storybook/addon-docs';

import PasswordStrengthIndicator from '@sb-ui/components/atoms/PasswordStrengthIndicator';

<Meta
  title="atoms/PasswordStrengthIndicator"
  component={PasswordStrengthIndicator}
/>

# PasswordStrengthIndicator

Component `PasswordStrengthIndicator` render four color state due to 'value' strength

export const Template = (args) => <PasswordStrengthIndicator {...args} />;

1. Empty value `''`
2. Easy `'easy'`
3. Medium `'Medium123'`
4. Hard `'Hard123###'`

<Canvas>
  <Story
    name="Empty text"
    args={{
      value: '',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Easy"
    args={{
      value: 'easy',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Medium"
    args={{
      value: 'Medium123',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Hard"
    args={{
      value: 'Hard123###',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
