import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Embed from '@sb-ui/pages/User/LearnPage/BlockElement/Embed';

<Meta title="Blocks/Embed" component={Embed} />

# Embed

Component `Embed` should use to display video (accepts data options `caption`, `embed`, `height`)

export const Template = (args) => <Embed {...args} />;

<Canvas>
  <Story
    name="Embed"
    args={{
      content: {
        data: {
          caption: 'Test video caption',
          embed: 'https://www.youtube.com/embed/dQw4w9WgXcQ',
          height: '350',
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
