import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { LearnContextDecorator } from '@sb-ui/stories/decorators';
import Bricks from '@sb-ui/pages/User/LearnPage/BlockElement/Bricks';

<Meta
  title="Blocks/Bricks/Student/Solved"
  decorators={[
    (Story) => (
      <LearnContextDecorator>
        <Story />
      </LearnContextDecorator>
    ),
  ]}
  component={Bricks}
/>

# Bricks

Component `Bricks` should use to display BricksAnswer `answer: { words: StringArray } reply: { words: StringArray}` (accepts data options `question`, `words`)

export const Template = (args) => <Bricks {...args} />;

<Canvas>
  <Story
    name="Correct"
    args={{
      answer: {
        words: ['a', 'b', 'c', 'd', 'e', 'f'],
      },
      reply: {
        words: ['a', 'b', 'c', 'd', 'e', 'f'],
      },
      content: {
        data: {
          question: 'Design should be okay.',
          words: ['a', 'c', 'd', 'b', 'e', 'f'],
        },
      },
      isSolved: true,
      blockId: 'Test id',
      revision: 'Test revision',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Wrong"
    args={{
      answer: {
        words: ['a', 'b', 'c', 'd', 'e', 'f'],
      },
      reply: {
        words: ['a', 'b', 'c'],
      },
      content: {
        data: {
          question: 'Design should be okay.',
          words: ['a', 'c', 'd', 'b', 'e', 'f'],
        },
      },
      isSolved: true,
      blockId: 'Test id',
      revision: 'Test revision',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
